# Create a file called Procfile (no extension)
```

Paste this into `Procfile`:
```
web: uvicorn main:app --host 0.0.0.0 --port $PORT
```

---

## **Verify your folder structure looks like this:**
```
linkguard/
└── backend/
    ├── main.py
    ├── requirements.txt
    └── Procfile